{{- if .Values.webhook.enabled }}
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: {{ include "ci-operator.namespace" . }}/ci-serving-cert
  name: ci-mutating-webhook-configuration
webhooks:
  - admissionReviewVersions:
      - v1
      - v1beta1
    clientConfig:
      service:
        name: ci-webhook-service
        namespace: {{ include "ci-operator.namespace" . }}
        path: /mutate-ci-w6d-io-v1alpha1-play
    failurePolicy: Fail
    name: mplay.kb.io
    rules:
      - apiGroups:
          - ci.w6d.io
        apiVersions:
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - plays
    sideEffects: None
{{- end }}