apiVersion: v1
kind: Service
metadata:
  name: {{ include "chopper.fullname" . }}
  labels:
    app: {{ include "chopper.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
  selector:
    app: {{ include "chopper.name" . }}
