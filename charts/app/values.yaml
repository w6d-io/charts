---
replicaCount: 1

name: "-"

id: ""
namespace: ""

kind: ""

# Main workload (Deployment/StatefulSet/DaemonSet/Job/CronJob from `kind`).
# Disable to render only extraResources.
workload:
  enabled: true

# CronJob options, used only when kind is cronjob (or cron). Rendered by
# templates/cronjob.yaml; the pod template reuses the same values as the other
# kinds (image, env, secrets, configs, vault annotations, serviceAccount,
# resources, volumes, initContainers, nodeSelector/tolerations/affinity).
# For this kind, Service and Ingress are never rendered, and the linkerd
# annotation is forced to "disabled" unless linkerd.enabled is true (an
# injected proxy keeps Jobs from ever reaching Complete).
cronjob:
  # Cron schedule, required when kind is cronjob. e.g. "30 2 * * *"
  schedule: ""
  # IANA time zone for the schedule (requires Kubernetes >= 1.27), e.g. "Etc/UTC".
  # Empty = controller-manager local time (UTC on managed clusters).
  timeZone: ""
  # Forbid: skip a run while the previous one is still running (safe batch default)
  concurrencyPolicy: Forbid
  # Seconds past schedule time a late run may still start before counting as missed
  startingDeadlineSeconds: 300
  suspend: false
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  # Pod restartPolicy of spawned Jobs: Never (recommended) or OnFailure
  restartPolicy: Never
  # Job spec knobs (rendered under jobTemplate.spec via _job_spec.yaml)
  backoffLimit: 2
  # ttlSecondsAfterFinished: 86400
  # activeDeadlineSeconds: 3600
  # parallelism: 1
  # completions: 1

# Optional bundled PostgreSQL (bitnami subchart). Disabled by default; enable
# and configure under this key to deploy a database alongside (or instead of)
# the app workload. See https://github.com/bitnami/charts/tree/main/bitnami/postgresql
postgresql:
  enabled: false

database:
  host: ""
  port: 5432
  adminpassword: ""
  password: ""
  postgres_password: ""
  component: ""
  enabled: false

generateSecret:
  enabled: false

liquibase:
  enabled: false

# Multi-tenant single-pod migration (opt-in; disabled so other apps are unaffected).
# When enabled, a controller Job runs as an ArgoCD PreSync hook: it reads the tenant
# list baked into the chart, then runs one Liquibase Job per tenant (from the
# `liquibase` library, Vault creds) by batch, isolating per-tenant failures and gating
# on a failure threshold. So the single Application does: PreSync (migrate all tenants)
# -> Sync (roll out the pod). The `-db` liquibase image reuses `.Values.image`.
multiTenantMigration:
  enabled: false
  # Comma-separated list of tenant identifiers to migrate (e.g. "alpha,beta,gamma").
  # Each identifier maps to a database "<tenant>.<database.dbSuffix>" and to Vault
  # static-creds "<tenant>-<vault.roleSuffix>". Rendered into the migration ConfigMap
  # as a file (one per line) that the controller reads -- no external call at sync time.
  # When empty, falls back to .Values.kafka.tenants so both lists can share one source.
  tenants: ""
  batchSize: 25                                # max concurrent child Jobs
  failThresholdPct: 10                         # controller exits non-zero above this
  jobTimeoutSeconds: 600                       # per child Job
  controller:
    image: "w6dio/kubectl:1.6.0-slim"
    serviceAccountName: ""                     # default: <app>-migration
    activeDeadlineSeconds: 3600
    ttlSecondsAfterFinished: 600
  child:
    serviceAccountName: "time"                 # SA bound to the Vault role below
    job:
      backoffLimit: 1
      activeDeadlineSeconds: 600
      ttlSecondsAfterFinished: 600
  database:
    host: "vault:strada/data/common/postgres#url"  # plain or vault-env ref
    port: 5432
    dbSuffix: "time"                           # DB per tenant = "<tenant>.<dbSuffix>"
  vault:
    role: "time"
    addr: "http://vault.vault:8200"
    skipVerify: true
    dbMount: "strada-db"
    roleSuffix: "time-db"                       # DDL static-role = "<tenant>-<roleSuffix>"

strategy: {}
#  type: Recreate
# Rollout sample
# canary:
#   maxSurge: 3
#   maxUnavailable: 0
#   canaryService: app-canary
#   stableService: app
#   steps:
#     - setWeight: 50
#     - pause: {}

revision:
  enabled: false

image:
  repository: 'nginx'
  tag: 'alpine'

serviceAccount:
  create: true
  token: true
  annotations: {}

role:
  rules: null

podSecurityPolicy:
  enabled: false

podAnnotations: {}
podLabels: {}

defaultContainer: ""

lifecycle:
  enabled: false

resources: {}

# Container-level securityContext, applied to the main container of the workload
# (Deployment/StatefulSet/DaemonSet/Rollout) and of the CronJob pod template.
# Pod-level securityContext stays under `securityContext`. Example:
#   containerSecurityContext:
#     allowPrivilegeEscalation: false
#     readOnlyRootFilesystem: true
containerSecurityContext: {}

service:
  enabled: true
  name: "-"
  internalPort: 8080
  externalPort: 8080
  type: ClusterIP

externalsvc: {}

ingress:
  enabled: false
  externaldns: null
  host: ''
  path: /
  issuer: ''
  class: 'nginx'
  prefix: 'nginx.ingress.kubernetes.io'
  className: ''         # ingressClassName field https://kubernetes.github.io/ingress-nginx/#what-is-ingressclassname-field
  extraPaths: []
  annotations: {}
  secretName: ''

cert:
  enabled: false
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: ""
  dnsNames: []

metrics:
  enabled: false
  path: '/metrics'
  port: 8080

env: ""
extraEnv: ""

configs: []

secrets: []

nodeSelector: {}

volumeMounts: {}
volumes: {}
extraVolumes: []
extraVolumeMounts: []
autoscaling:
  enabled: false

extraResources: ""

mongodb:
  install: false

redis:
  install: false

pdb:
  enabled: false

vault:
  enabled: true
  url: http://vault.vault:8200
  envFrom:
    enabled: false
    path: strada/data
    # Additional KV paths appended (comma separated) to vault-env-from-path.
    # Keys found in later paths override keys from earlier ones.
    # extraPaths:
    #   - strada/data/feature/myapp/my-branch-slug
    extraPaths: []

linkerd:
  enabled: false
  policy:
    port: ""
    proxyProtocol: "unknown"
    # Multi-port support (takes precedence over port/proxyProtocol when set)
    # ports:
    #   - port: 8080
    #     proxyProtocol: "HTTP/2"
    #   - port: 9090
    #     proxyProtocol: "HTTP/1.1"
    ports: []
  # Monitoring identity (auto-allowed on metrics port when metrics + communications are enabled)
  monitoring:
    app: monk8s-prometheus
    namespace: monitoring
  # Ingress controller namespaces (auto-allowed when ingress + communications are enabled)
  ingressNamespaces:
    - ingress-nginx
  # List of apps allowed to communicate (enables deny mode when set)
  # communications:
  #   - app: frontend
  #     namespace: my-ns
  communications: []

httproute:
  enabled: false
  host: ''
  gateway: ''
  path: '/'
  rewrite: false
  rewritepath: '/'

backendpolicy:
  securityPolicy: 'gke-regional'
  logging: true
  sampleRate: 1000000

initContainers: []

healthcheck:
  enabled: false
  config:
    port: 80
    type: HTTP  # Can be HTTP, HTTPS, or TCP
    path: /
    host: ""
    timeout: "5s"
    checkInterval: "10s"
    healthyThreshold: 2
    unhealthyThreshold: 3
