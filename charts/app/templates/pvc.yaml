{{- if .Values.persistence -}}
  {{- $labels := (include "common.labels.standard" .) }}
  {{- $fullname := (include "global.names.fullname" .) }}
{{ range .Values.persistence }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- $labels | nindent 4 }}
  name: {{ $fullname }}-{{ .name }}
spec:
  accessModes:
  - {{ default "ReadWriteOnce" .mode }}
  resources:
    requests:
      storage: {{ default "1Gi" .size }}
  {{- include "common.storage.class" ( dict "persistence" . ) | nindent 2 }}
  volumeMode: {{ default "Filesystem" .volumeMode }}
{{- end -}}
{{- end -}}
