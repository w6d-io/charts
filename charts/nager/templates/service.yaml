apiVersion: v1
kind: Service
metadata:
  name: {{ include "nager.fullname" . }}
  labels:
    app: {{ include "nager.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      name: http
  selector:
    app: {{ include "nager.name" . }}
