apiVersion: v1
kind: Service
metadata:
  name: {{ include "jimbee.fullname" . }}
  labels:
    app: {{ include "jimbee.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      name: http
  selector:
    app: {{ include "jimbee.name" . }}
