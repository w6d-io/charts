apiVersion: v1
kind: Service
metadata:
  name: {{ include "jimbe.fullname" . }}
  labels:
    app: {{ include "jimbe.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      name: http
  selector:
    app: {{ include "jimbe.name" . }}
