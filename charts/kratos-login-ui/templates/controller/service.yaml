apiVersion: v1
kind: Service
metadata:
  name: {{ include "kratos-login-ui.fullname" . }}
  labels:
    {{- include "kratos-login-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kratos-login-ui.selectorLabels" . | nindent 4 }}
