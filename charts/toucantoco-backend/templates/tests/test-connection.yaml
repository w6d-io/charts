apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "toucantoco-backend.fullname" . }}-test-connection"
  labels:
    {{- include "toucantoco-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "toucantoco-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
