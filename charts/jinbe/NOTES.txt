Thank you for installing jimbe!

Your deployment name is: {{ .Release.Name }}

{{- if .Values.ingress.enabled }}

You can access the jimbe frontend through the following Ingress address:

  URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}{{ .Values.ingress.path }}

Ingress details:
  Host:        {{ .Values.ingress.host }}
  Path:        {{ .Values.ingress.path }}
  TLS:         {{ if .Values.ingress.tls }}Enabled (Secret: {{ .Values.ingress.tlsSecret }}){{ else }}Disabled{{ end }}
  Class:       {{ .Values.ingress.ingressClassName }}

Make sure your DNS is correctly configured to point to your Ingress controller's IP.

{{- else }}

Ingress is currently disabled.

You can access the application using the following service details (internal cluster only):

  Service Name: {{ include "jimbe.fullname" . }}
  Namespace:    {{ .Release.Namespace }}
  Port:         {{ .Values.service.port }}

To port-forward and access it locally, you can run:

  kubectl port-forward svc/{{ include "jimbe.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Then access the app via:

  http://localhost:{{ .Values.service.port }}

{{- end }}

---
jimbe Helm Chart version: {{ .Chart.Version }}
App version: {{ .Chart.AppVersion }}
Project: https://github.com/w6d-io/jimbe
