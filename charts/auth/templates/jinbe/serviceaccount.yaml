{{- if and .Values.jinbe.enabled .Values.jinbe.serviceAccount.create -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- with .Values.jinbe.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "helper.labels.standard" . | nindent 4 }}
  name: {{ include "app.jinbe.serviceAccountName" . }}
{{ if .Values.jinbeserviceAccount.token  }}
---
apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  annotations:
    kubernetes.io/service-account.name: {{ include "app.jinbe.serviceAccountName" . }}
  name: {{ printf "%s-token" (include "app.jinbe.serviceAccountName" .) }}

{{- end }}
{{- end }}
